:tddium:
  :environment:
    'BROCCOLI_ENV': test

  :headless: true

  :nodejs:
    :version: 0.11.11

  :phantomjs:
    :version: 1.9.0

  :firefox: 27.0.1

  :hooks:
    :pre: npm config set strict-ssl false && npm install ember-cli && npm install bower && npm install && ./node_modules/bower/bin/bower -F install

  :tests:
    - ./node_modules/ember-cli/bin/ember test
